import { FC, ReactElement, useState } from 'react';

const SpacesPage: FC = (): ReactElement => {
  return (
    <div>
      <section>
        <h1>I am the spaces page, you should be logged in to see me</h1>
      </section>
    </div>
  );
};

export default SpacesPage;
